blueprints:

  # Lambda function
  - stackname: amibaker-stack
    template: amibaker.template
    Capabilities: CAPABILITY_IAM

  # Example
  - stackname: example-stack
    template: example/bake.template
    parameters:
      AmiBaker: '{output:amibaker-stack:AmiBakerArn}'